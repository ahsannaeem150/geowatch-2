// React Native CLI autolinking config.
// `assets` links the bundled fonts into the Android app at build time
// (copies into android/app/src/main/assets/fonts).
module.exports = {
  assets: ['./assets/fonts'],
};
